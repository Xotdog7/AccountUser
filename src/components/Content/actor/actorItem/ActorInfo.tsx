import React, { FC } from "react";
import { Actor } from "../../../../types";

type Props = {
  actor: Actor;
};

const ActorInfo: FC<Props> = ({ actor }) => {
  return (
    <div>

    </div>
  );
};

export default ActorInfo;
